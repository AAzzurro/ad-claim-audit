# 短视频广告虚假宣传话术检测 — 进度记录

## 当前默认

本地权重在 `models/`，启动时优先读这里：

- **ASR**：FunASR `SenseVoiceSmall`
- **OCR**：PP-OCRv6 `small` 检测 + 识别

```
python -m src.pipeline --only 1,10 --force
```

规则重洗（不跑模型）：

```
python -m src.pipeline --only 1,10 --skip-asr --skip-ocr --force
```

`--force` 只重跑未 `--skip-*` 的模态；skip-ocr 时会读已有 `data/ocr/{id}.json` 再套过滤。

## Done

- 环境、抽音/抽帧、ASR/OCR、紧凑 `data/merged/{id}.json`（`audio_text` + `visual_text`）。
- 已切到 SenseVoice + OCR small，并删掉 Whisper / OCR medium 权重。
- **2026-09-02 重跑 `1.mp4`、`10.mp4`（2/2 成功）**
  - 口播可用：`1` 有「大女主 / 子宫 / 绝后 / 2号链接」；`10` 是「山茶花 / 五折」，不再是「三杂花」。
- **2026-09-02 OCR 清洗可进 e1**（不重跑模型，口播不变）
  - `visual_text`：`1` 994→**77** 字；`10` 1168→**306** 字。
  - `1` 留下商品卡：券后价¥193、全新二代、还原好气色、近30天低价、正品。买家秀长评、公屏、榜单已去掉。画面里几乎没有「气垫」（只出现在弹幕/评价里，已滤掉；口播也不提，e1 靠口播「子宫/绝后」）。
  - `10` 留下促销/卖点：红山茶花精华、五折、买尿裤送车、性价比之王、天花板、全网热销。抽奖、进场、公屏、赠品墙已去掉。还剩几条「山茶裤系列」的 OCR 复读和「当子山 / 折盟」残字，关键词基线可以容忍。
  - 过滤不再写死「气垫/山茶」。左下公屏、左上抽奖、底栏赠品按框丢掉，但带价/折/券/之王等卖点的行放行（商品卡和弹幕带重叠，不能整块裁）。

## 数据集

- `videos/`：237 条约 30 秒 mp4。目前只抽了 `1`、`10`。
- 标签表 `videos/output.xlsx`：类别不是任务书 6 类，也没有「正常」样本。先不处理。

## 弹幕区域能否在 OCR 前裁掉？

**不建议整块遮罩/裁切。** 公屏在左下，商品卡/券后价/促销叠字在同一高度。OCR 前涂黑会丢掉卖点，还得重跑检测。现在只在 `src/merge.py` 事后按文本 + 弱位置过滤。

## 下一步（只做这一件）

**e1：关键词/规则基线。** 不抽新视频。在已有 `audio_text + visual_text` 上用虚假宣传常用短语打标（绝对化、医疗承诺、功效保证），先只跑 `1`、`10`。

注意：不要用单字「最」——`1` 的「最高领取20元」会误中。用短语（最好、第一、国家级、之王、天花板、根治、无效退款…）。看命中是否落在口播承诺 / 画面卖点上，而不是控件残字。
